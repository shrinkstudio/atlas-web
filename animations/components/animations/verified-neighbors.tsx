// SLOT: paste the "Verified Neighbours" v0 component below (replace this whole file).
// Keep a default export named VerifiedNeighbors. URL when deployed: /verified-neighbors

export default function VerifiedNeighbors() {
  return (
    <div
      style={{
        fontFamily: "DM Mono, monospace",
        color: "#7059ff",
        fontSize: 12,
        letterSpacing: 2,
        textTransform: "uppercase",
      }}
    >
      verified-neighbors — paste v0 component
    </div>
  );
}
