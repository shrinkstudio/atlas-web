// SLOT: paste the "Block Events" v0 component below (replace this whole file).
// Keep a default export named BlockEvents. URL when deployed: /block-events

export default function BlockEvents() {
  return (
    <div
      style={{
        fontFamily: "DM Mono, monospace",
        color: "#7059ff",
        fontSize: 12,
        letterSpacing: 2,
        textTransform: "uppercase",
      }}
    >
      block-events — paste v0 component
    </div>
  );
}
