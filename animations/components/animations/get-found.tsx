// SLOT: paste the "Get Found" v0 component below (replace this whole file).
// Keep a default export named GetFound. URL when deployed: /get-found

export default function GetFound() {
  return (
    <div
      style={{
        fontFamily: "DM Mono, monospace",
        color: "#7059ff",
        fontSize: 12,
        letterSpacing: 2,
        textTransform: "uppercase",
      }}
    >
      get-found — paste v0 component
    </div>
  );
}
