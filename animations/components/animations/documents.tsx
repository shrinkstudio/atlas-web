// SLOT: paste the "Documents" v0 component below (replace this whole file).
// Keep a default export named Documents. URL when deployed: /documents

export default function Documents() {
  return (
    <div
      style={{
        fontFamily: "DM Mono, monospace",
        color: "#7059ff",
        fontSize: 12,
        letterSpacing: 2,
        textTransform: "uppercase",
      }}
    >
      documents — paste v0 component
    </div>
  );
}
