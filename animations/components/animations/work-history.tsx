// SLOT: paste the "Work History" v0 component below (replace this whole file).
// Keep a default export named WorkHistory. URL when deployed: /work-history

export default function WorkHistory() {
  return (
    <div
      style={{
        fontFamily: "DM Mono, monospace",
        color: "#7059ff",
        fontSize: 12,
        letterSpacing: 2,
        textTransform: "uppercase",
      }}
    >
      work-history — paste v0 component
    </div>
  );
}
